/* ─────────────────────────────────────────────────────────────
   모니터링 연결 설정 — 여기 한 줄만 고치면 됩니다.

   1. apps-script.gs 파일의 설정 순서를 따라 하면 마지막에
      https://script.google.com/macros/s/AKfy....../exec  같은 주소가 나옵니다.
   2. 그 주소를 아래 따옴표 안에 붙여 넣고 저장 → 커밋 → 푸시.

   비워 두면 모니터링만 꺼지고, 게임은 지금과 똑같이 잘 돌아갑니다.
   (인터넷이 끊겨도 마찬가지입니다. 게임은 태블릿 안에서 혼자 돌아갑니다.)
   ───────────────────────────────────────────────────────────── */

window.SYNC_URL = '';

/* 모니터링 화면에서 몇 초마다 새로고침할지 (기본 5초) */
window.SYNC_POLL_SEC = 5;

/* 몇 분 동안 아무 활동이 없으면 "멈춤"으로 볼지 (기본 5분) */
window.SYNC_IDLE_MIN = 5;

/* ─────────────────────────────────────────────────────────────
   게임 아케이드 저장소 설정 (arcade.html 용) — 여기 한 줄만 고치면 됩니다.

   1. arcade-apps-script.gs 파일의 설정 순서를 따라 하면 마지막에
      https://script.google.com/macros/s/AKfy....../exec  같은 주소가 나옵니다.
      (탐정단 모니터링과는 다른 스프레드시트·다른 배포입니다. 주소도 다릅니다.)
   2. 그 주소를 아래 따옴표 안에 붙여 넣고 저장 → 커밋 → 푸시.

   비워 두면 학생이 올린 게임이 그 태블릿 안에만 저장됩니다
   (그 기기에서는 새로고침해도 남지만, 다른 기기에서는 안 보입니다).
   ───────────────────────────────────────────────────────────── */

window.ARCADE_URL = 'https://script.google.com/macros/s/AKfycbxLADz8SD1G1FldY4kMxK0lvOtxIE806GntbubgSygH6u4gqIVrnOq4gZv2NrLMkBws5A/exec';
